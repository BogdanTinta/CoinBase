import { StyleSheet } from 'react-native'

import { metrics } from '../../../constants/metrics'

export const newsletterStyle = StyleSheet.create({
    container: {
        flex: metrics.size1,
    },
})